// config

// User name of the voter
const USERNAME = 'YOUR-USERNAME';

// Password of the voter
const PASSWORD = 'YOUR-PASSWORD';

// minimum balance
const MIN_ACCOUNT_BALANCE = 100;

module.exports = {
    username         : USERNAME,
    password         : PASSWORD,
    minAccountBalance: MIN_ACCOUNT_BALANCE
};
